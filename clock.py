import time



alarm = sys.arv[1].split(':')
now=time.localtime()
alarmM=int(alarm[1])
alarmH=int(alarm[0])
While now.tm_min != alarmM or now.tm_hour != alarmH
        time.sleep(60)
        now=time.localtime
        print(now.tm_hour,':', now.tm_min)
print('***')

current = time.strftime('%H:%M')
alarm = sys.argv[1]
while current != alarm:
        time.sleep(5)
        print(current)
        current = time.strftime('%H:%M')
        alarm = sys.argv[1]
print("now\!")
/bin/bash: import: command not found
        current = time.strftime('%H:%M')
        alarm = sys.argv[1]
print("now\!")

